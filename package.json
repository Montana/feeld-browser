{
  "name": "feeld-browser-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Local browser-based remote control for the official Feeld Android app through ADB.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "sharp": "^0.34.3"
  }
}
